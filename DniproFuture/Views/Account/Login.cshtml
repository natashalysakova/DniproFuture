@model DniproFuture.Models.LoginViewModel

@{
    ViewBag.Title = "Admin: Login";
}

<div class="container">
    <h2>Login</h2>
    <p>Please log in to access the administrative area:</p>

    @using (@Html.BeginForm())
    {
        @Html.ValidationSummary(true)
        @Html.EditorForModel()
        <p><input type="submit" value="Login" /></p>
    }
</div>